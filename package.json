{
  "name": "archtrack-server",
  "version": "1.0.0",
  "description": "ArchTrack Server - Full Stack Application",
  "main": "dist/server.js",
  "scripts": {
    "postinstall": "pnpm --dir apps/server install && pnpm --dir apps/client install",
    "build": "pnpm --dir apps/server run build && pnpm --dir apps/client run build",
    "start": "pnpm --dir apps/server run start",
    "dev": "pnpm --dir apps/server run dev",
    "dev:client": "pnpm --dir apps/client run dev",
    "dev:server": "pnpm --dir apps/server run dev",
    "install:all": "pnpm --dir apps/server install && pnpm --dir apps/client install",
    "build:server": "pnpm --dir apps/server run build",
    "build:client": "pnpm --dir apps/client run build",
    "type-check": "pnpm --dir apps/server run type-check && pnpm --dir apps/client run type-check",
    "lint": "pnpm --dir apps/server run lint && pnpm --dir apps/client run lint",
    "format:check": "pnpm --dir apps/client run format:check",
    "format": "pnpm --dir apps/client run format",
    "test": "pnpm --dir apps/server test && pnpm --dir apps/client test",
    "test:coverage": "pnpm --dir apps/server run test:coverage && pnpm --dir apps/client run test:coverage",
    "prepare": "husky install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.3.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}