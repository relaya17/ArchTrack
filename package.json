{
  "name": "archtrack-server",
  "version": "1.0.0",
  "description": "ArchTrack Server - Full Stack Application",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && echo 'TypeScript compiled successfully'",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "build:full": "npm run build && cd apps/client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.3.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}